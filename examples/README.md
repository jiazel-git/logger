# jzlog 使用示例

本目录包含 jzlog 日志库的使用示例。

## 示例列表

### basic_usage.cc
演示日志库的基本使用方法，包括：
- 创建日志器
- 添加输出 sink
- 记录不同级别的日志
- 格式化输出

## 编译示例

```bash
# 在项目根目录下
cd /path/to/logger
mkdir -p build && cd build
cmake ..
make
```

## 运行示例

```bash
# 运行基本示例
./bin/basic_usage
```
